import { BaseCard } from "vlocity_cmt/baseCard";                    import { LightningElement, api, track } from "lwc";                    import data from "./definition";                    export default class cfCommsB2BAssetManagerCard_1_JoeMcMaster extends BaseCard(LightningElement) {                        connectedCallback() {                            this.setDefinition(data);                        }                    }